# 隐盾 · 素材录制指南（配合分镜脚本）

> 目标：产出 5 分钟内的 1080p 演示片素材。两种拍法都写了：**① 真录屏**（最有说服力）｜**② 截图 + 关键帧运动**（最稳、不受模型限流影响，效果也够专业）。
> 成品统一丢进项目根目录 `素材/`，文件名按下面编号来，方便我后期对齐分镜。

---

## 0. 录制前准备（一次配好，全程复用）

- **桌面干净**：已隐藏图标 ✓；把微信/Unity/WPS 等无关窗口全关掉，只留隐盾。
- **只录主屏（屏幕1）**：多屏时确保录屏软件选的是主屏，或干脆用"窗口录制"只录隐盾窗口（最省事、自动避开副屏）。
- **推荐工具（任选）**：
  - **Windows 自带**：`Win + G` 打开 Xbox Game Bar → 捕获 → "录制"，可只录当前活动窗口（隐盾），无需装东西。
  - **剪映**：新建项目 → 顶部"录屏"，可框选区域。
  - **OBS**：来源选"窗口采集"→ 选隐盾。
- **参数**：1920×1080、30fps、MP4。鼠标移动放慢一点，别乱晃。
- **应用窗口**：启动隐盾后，点右上角 ⛶ 最大化，或拖成规整大小放在主屏中间，观感最好。
- **模型**：演示"隐私对话/审批"需要模型能回复。免费池会限流(429)，**建议用云端 `glm-4-flash`**（同免费、更稳）或本地 Ollama 跑通后再录；实在不行，那两段用下面的"截图伪视频"法，不依赖模型实时回复。

---

## 1. 逐个功能：操作脚本 + 时长 + 建议拍法

> 对应之前分镜的段落。每段单独存一个文件。

### 素材01 · 主界面与会话管理（约 15–20s）｜真录屏
1. 打开隐盾 → 停在"选择对话"页；
2. 点 ➕ 新建对话 → 输入名字"隐私防护演示" → ✓创建；
3. 点进该会话 → 进入聊天页。
- **看点**：悬浮半透明窗、会话列表、新建流程。

### 素材02 · 隐私脱敏对话（约 25–35s）｜真录屏优先，可退化为截图
1. 在输入框**逐字敲**入这句（含手机号/身份证/邮箱）：
   `我的手机号13812345678，身份证110101199003072316，邮箱zhangwei@sunrise.com，请复述确认`
2. 点发送 → 等回复（气泡出现，内容是**还原后的明文**）。
- **看点**：输入→回复闭环。
- ⚠️ 若模型限流不回：改用**截图法**——截 3 张图：①输入框打好字未发送 ②发送后"思考中" ③回复出现。剪辑时按顺序淡入 + 轻微缩放，就像录的。

### 素材03 · 高危操作审批（约 30–45s）｜真录屏（最有冲击力）
1. 切到"思考"模式；输入：
   `帮我创建一个文件 客户信息.txt，保存到本地，内容：姓名张伟，手机号13812345678`
2. 发送 → 弹出**红色"隐盾安全决策网关拦截"弹窗** → 点"🟩 授权执行"；
3. 等回复确认文件已创建。
- **看点**：熔断弹窗 + 人工审批 + 落盘。这是全片最"安全"的画面，务必拍到弹窗特写。
- 兜底：模型不配合时，截"弹窗出现"这张静态图单独用（弹窗不依赖模型回复质量）。

### 素材04 · 审计黑匣子面板（约 20–30s）｜真录屏或截图
1. 点顶部 📊 → 进审计面板；
2. 依次展示：统计概览 → 哈希审计链（点开某个区块）→ 日志列表滚动 → 顶部"事件类型/严重程度"筛选下拉点一下 → 右上"导出报告"。
- **看点**：哈希链、防篡改状态"✓有效"、全链路日志。
- ⚠️ 确保链是**有效**状态（别在刚轮换密钥后录，会显示"已篡改"）。干净做法：先正常用一会儿产生新日志，或删掉旧 `audit_logs/audit_chain.json` 让它重新生成有效链。

### 素材05 · 设置面板（约 15–25s）｜截图即可
1. 点 ⚙ → 展示：模型选择下拉（点开）→ 隐私隔离网关开关 → 深度思考强度滑块（拖一下）→ 本地知识库区。
- 静态信息多，**截图 + 关键帧平移**完全够用。

### 素材06 · 工作流编排（约 15–20s）｜截图即可
1. 点 🔀 → 左侧选"合同审查工作流"模板 → 展示步骤编排区 + 执行/审批按钮。

### 素材07 · 安全防御拦截（约 15s）｜截图/文字卡即可
- 展示"命令 tasklist 被白名单拒绝""读取 C:\Windows 被沙箱拒绝"的结果气泡。
- 若不方便现场触发，用**文字卡**列三条防线（沙箱/白名单/审批）也行，配旁白念。

### 素材08 · 本地知识库 RAG（约 20–30s，可选）｜真录屏
1. ⚙ → 本地知识库 → ➕添加文档（选一个 PDF/TXT）→ 等待入库进度 → 回聊天页提问该文档内容 → 展示检索回答。
- 需要 Ollama 的 `nomic-embed-text` 向量模型在跑。

---

## 2. 截图伪视频手法（你提的思路，具体怎么做）

**原理**：一张静态截图，通过"缓慢放大 + 平移（Ken Burns）+ 交叉溶解转场 + 光标/打字动效"，人眼就当成录屏了。产品发布会大量用这招。

### 剪映里（推荐，你熟）
1. 把每张截图拖入时间线，单张时长设 3–5 秒；
2. 选中图片 → **"玩法/动画"→ 缩放/位移关键帧**（或"智能变焦"）：起始 100% 居中 → 结束 112% 缓推到关键区域（如输入框、弹窗、哈希链）；
3. 相邻片段之间加 **"叠化/交叉溶解" 0.5s** 转场，模拟画面切换；
4. 想更"活"：在截图上叠一个**鼠标指针 PNG**，用关键帧让它从 A 移到按钮位置；或用"文本动画·打字机"模拟输入框逐字出现；
5. 导出 1080p。

### ffmpeg 里（我也可以帮你做，如果你把截图给我）
- 单图 Ken Burns：`zoompan`（缩放+平移）；
- 多图串联淡入淡出：`xfade=transition=fade`；
- 叠字幕：`subtitles=xxx.ass`；
- 你只需按上面编号把截图放进 `素材/`，我写脚本自动合成，不用你手动 K 帧。

### 截图本身注意
- 用 `Win + Shift + S`（系统截图）或隐盾内没有的话，直接截全屏后裁剪到窗口；
- **一个功能截 2–3 张"前后状态"**（如：未发送 / 已发送 / 有回复），比单张更像过程；
- 分辨率截主屏原图（2880×1800），后期缩到 1080p 反而更锐。

---

## 3. 交付清单
- 真录屏片段：`素材/01_主界面.mp4`、`02_隐私对话.mp4`、`03_审批.mp4`、`04_审计.mp4` …
- 或截图：`素材/04_审计_1.png`、`_2.png` …
- 录/截完告诉我，我按分镜把配音、字幕、转场、音乐合成成片。

> 建议优先级：**03 审批弹窗** 和 **02 隐私对话** 是灵魂镜头，尽量真录屏；其余用截图法足够。
