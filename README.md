# 🛡️ 隐盾安全智能体 (Yindun Security Agent) - V2.0 Complete

欢迎来到 **隐盾安全智能体 (V2.0 完全解耦版)** 官方代码库。

本应用是一款常驻系统桌面最上层的原生半透明悬浮智能体客户端。项目经历了两轮工业级深度解耦重构，主窗体（MainWindow）已彻底蜕变为纯粹的“至简骨架大连廊”，前台所有核心视舱（大厅、状态栏、控制台、设置页、气泡、弹窗）均已实现 100% 原子组件化封装，具备极高的内聚度与健壮的信号量互锁机制。

---

## 🎨 核心功能特性

1. **🔒 内存级数据隐私网关**：内置物理脱敏隔离网关（`PrivacyEngine`），在涉密文本砸向本地算力底座前，在内存中就地执行实体模糊化与数据匿名化，并在汇报给人类时安全还原明文。
2. **🧠 纯本地多模态算力**：全链路采用纯离线私有化部署的 `Ollama` 算力集群（默认驱动 `qwen2.5:7b`），通过 `NO_PROXY` 管道确保全隔离运行，绝不向任何云端上传一行密文。
3. **📂 离线文档全自动挂载**：支持离线 PDF、Word (docx)、Excel (xlsx/xls)、TXT、MD、CSV 等多种机密办公文档的跨平台编码自适应提取。
4. **🚨 熔断级越界操作拦截**：当智能体企图调动物理机械臂工具并切入非默认工作区或系统核心敏感目录时，系统将强行触发独立的二次确认拦截弹窗，安全阻塞后台写盘线程，强行等待人类安全员二次审批。
5. **💎 天花板级原生 UI/UX 体验**：
   - 强行注入无损硬件级高分屏缩放与抗锯齿策略，彻底抹除字体与圆角锯齿。
   - 微信/ChatGPT 同款纯原生弹性聊天气泡，锁定 **75% 最大宽度自适应换行算法**。
   - 物理级圆角卡片质感，为 AI 气泡和拦截弹窗注入细腻的**高斯半透明柔和物理阴影**。
   - 窗口不透明度全面升级为 100%~50% 现代横向滑动条（QSlider）控制。

---

## 📂 工业级全原子化解耦目录骨架

本系统严格遵循高级桌面端开发的“单一职责原则”，全盘目录结构及原子功能积木分布如下：

```text
隐盾项目根目录/
│
├── main_win.py            # 🚀 全局启动总入口（仅负责硬件抗锯齿配置、QSS皮肤注入与窗口点火）
├── 启动.bat               # ⚡ 工业级一键点火脚本（自动修复管理员路径，强推 UTF-8 编码页）
├── 准备环境.bat           # 🛠️ 环境合规部署网关（秒级创建虚拟隔离沙箱并利用国内高速镜像源补齐依赖）
│
├── gui/                   # 🎨 视觉表现与前端全原子组件舱
│   ├── styles.py          # 专门锁死所有高定制化 QSS 样式表及内嵌图源
│   ├── chat_bubble.py     # 💬 纯原生错落式卡片气泡组件与状态通知条（不含任何 HTML 代码）
│   ├── confirm_dialog.py  # 🚨 独立高危越界写盘人工二次确认拦截模态窗口
│   ├── chat_display.py    # 📊 独立消息大厅视舱（负责接管滚动布局、气泡动态追加与平滑自动触底）
│   ├── status_bar.py      # 🧠 独立动画状态条（内置影子大脑研判中...动态点水呼吸计数器）
│   ├── control_dock.py    # 📥 独立多模态控制台底座（组合管理下拉框、挂载键与发射按钮，对外提供状态锁）
│   ├── settings_panel.py  # ⚙️ 独立高级配置面板舱（负责完整的表单渲染，通过字典信号向外同步数据）
│   └── main_window.py     # 🏰 前端大管家主控制台（纯粹的组合框架，仅负责骨架拼接与跨组件信号路由转发）
│
├── threads/               # 🧠 后台异步子线程驱动舱
│   └── agent_worker.py    # 专门处理大模型 invoke、工具调用及网关拦截的异步 Worker 线程
│
├── utils/                 # 🛠️ 纯算法独立工具舱
│   └── document_parser.py # 专门负责 PDF/Word/Excel 跨平台文本自动提取与多重编码探测
│
└── core/                  # 🛡️ 隐盾安全业务核心（团队已有核心资产）
    ├── privacy_engine.py  # 数据匿名化与 deanonymize 还原矩阵
    └── file_tools.py      # 大模型绑定的 list、create、delete 本地物理工具链