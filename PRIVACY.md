# 隐盾 · 数据隐私脱敏能力说明

> 本文档说明隐盾安全智能体的隐私脱敏能力、已知取舍、密钥存储方式与回归检查方法，便于交接、运维与对外说明。

---

## 1. 脱敏能力清单

脱敏引擎按**业务分组**组织，命中敏感实体后被替换为不可预测的占位符（`[实体类型_序号_nonce]`），原文在本地用 Fernet 加密保存于映射表，最终展示给用户前还原为明文。

| 业务分组 | 覆盖实体类型 | 说明 |
| --- | --- | --- |
| **PII（个人身份）** | 手机号 PHONE、邮箱 EMAIL、身份证 IDCARD/IDCARD15、护照/通行证 PASSPORT、车牌 PLATE、IPv4/IPv6、MAC、姓名 NAME、地址 ADDRESS、微信号 WECHAT | 支持 +86/0086 国际前缀、空格/连字符/全角数字、中文域名、4 位分组等变体 |
| **PHI（个人健康）** | 病历号 MEDICAL_RECORD、医保卡号 MEDICAL_INSURANCE | 需"病历/医保"前缀触发 |
| **财务** | 银行卡 BANKCARD（Luhn 校验）、统一社会信用代码 CREDIT_CODE、金额 MONEY | 银行卡去分隔符后 Luhn 精验，降低误报 |
| **密钥** | APIKEY、私钥 PRIVATE_KEY、PEM、JWT、云平台密钥 CLOUD_KEY、口令 PASSWORD、连接串密码 CONN_STRING | 连接串/口令仅替换密码值、保留字段名 |
| **Token** | BEARER、ACCESS_TOKEN | 带 `Bearer`/`access_token=` 前缀触发 |
| **路径** | 敏感文件路径 FILE_PATH、业务敏感路径 SENSITIVE_PATH | 覆盖 `.ssh`/`.aws`/`.env` 等及含薪资/工资等词的业务路径 |

**精验机制**：正则粗筛 + 程序化精验，显著降低误报——手机号精确 11 位、银行卡 Luhn 校验、15 位身份证按地区码/触发词校验、车牌负向词判断、IPv6 先于 IPv4 匹配、金额左边界防截取。

---

## 2. 已知取舍（勿误判为缺陷）

以下场景**有意不脱敏**，属产品安全策略取舍，请勿在后续修改中强行放宽，以免反过来引入误伤：

1. **纯数字无单位金额不脱敏**（如"转账金额 1250000 已到账"）。避免误伤订单号/运单号/版本号等业务数字。金额规则仅匹配带人民币符号、逗号分隔或单位（万元/亿元/元）的明确金额形态。
2. **无触发词人名不脱敏**。"姓氏+2 字"误伤率高，弱规则 `NAME_WEAK_MODE` 默认关闭。**推荐**用 `engine.add_custom_names([...])` 导入通讯录做精确匹配。
3. **长度 < 8 的 sk- / Bearer token 不脱敏**（如 `sk-abc123`）。低于置信门槛，避免误吞普通短编号。
4. **DPAPI 保护下的密钥不可跨用户迁移**。Windows 下密钥经 `CryptProtectData` 包裹、绑定当前系统用户；**换用户 / 重装系统后历史会话占位符无法还原为明文**（安全优先于可恢复性）。
5. **2026-08-27 之前的 100 条审计记录完整性不可验证**。该日 HMAC 密钥被重建，旧签名密钥丢失。这些记录已归档为只读历史（详见 3.1），**不参与** `verify_chain()` 校验；8/27 之后的记录防篡改能力正常。

---

## 3. 密钥存储位置与迁移

密钥由 `yindun/core/secret_manager.py` 的 `SecretManager`（单例）统一管理，用于加密映射表、会话密文字段、自定义模型 API 密钥等。

**当前存储位置（第 5 步起，移出项目目录）：**

| 平台 | 密钥路径 |
| --- | --- |
| Windows | `%APPDATA%\Yindun\.secret_key` |
| 其他 | `~/.config/yindun/.secret_key` |

**迁移说明：**
- 旧位置 `yindun/cache/.secret_key`（项目目录内）若存在，首次启动会自动**复制**到新路径，并在控制台提示可手动删除。
- **当前状态（2026-09-03）**：旧文件已删除，项目目录内不再有任何明文密钥。
- **备份**：直接复制新路径下的 `.secret_key` 即可；注意 Windows 下该文件被 DPAPI 包裹，备份恢复须在**同一用户**下进行。

**安全措施：**
- Windows：权限用 `icacls "/inheritance:r" /grant:r "%USERNAME%:F"` 收紧（仅当前用户）。
- POSIX：`os.chmod(0o600)`。
- Windows DPAPI 包裹：文件内容 `CryptProtectData` 加密（`yindun/core/secret_manager.py`，开关 `SECRET_DPAPI=True`）。非 Windows 或 DPAPI 失败自动回退为明文密钥 + 权限收紧，保证能启动。

**审计 HMAC 密钥（2026-09-03 一并迁移）：**

| 平台 | 密钥路径 |
| --- | --- |
| Windows | `%APPDATA%\Yindun\.audit_hmac_key` |
| 其他 | `~/.config/yindun/.audit_hmac_key` |

- 由 `yindun/core/audit_log.py` 的 `_audit_key_file()` 定位，旧位置 `yindun/cache/.audit_hmac_key` 存在时自动**复制迁移**。
- ⚠️ **迁移必须复制同一把钥匙，绝不可重新生成**——换了密钥会导致历史审计链全部校验失败。
- 该密钥与它保护的 `audit_chain.json` **不可同盘存放**，否则审计的"防篡改"可被绕过（拿到密钥即可篡改并重算哈希链）。

### 3.1 审计链完整性

审计日志采用 HMAC-SHA256 哈希链（每条记录含 `previous_hash` + `entry_hash`），由 `AuditLog.verify_chain()` 校验完整性。

**历史事件（2026-08-27 链断裂）：**

审计 HMAC 密钥于 2026-08-27 被重新生成，导致此前 **100 条**（2026-07-24 ~ 2026-08-27）记录的签名密钥永久丢失、无法校验。由于哈希链从头串联，`verify_chain()` 自该日起一直返回 `False`——即审计的"防篡改"能力处于名义失效状态（日志照常写入，但无法验证是否被改动）。

**处置（2026-09-03，方案 A：归档 + 重开新链）：**

| 项 | 内容 |
| --- | --- |
| 归档 | 前 100 条原样导出为 `audit_logs/archive/audit_chain_legacy_20260827.json`（只读，`_archive_note` 标注完整性不可验证） |
| 重开 | 以一条 `chain_restart` 记录作为新链起点，8/27 之后的记录**保留原 timestamp** 重算哈希链 |
| 结果 | `verify_chain()` → `True`；实测篡改任意一条后立即返回 `False`，防篡改能力恢复 |

**运维要点：**
- `audit_logs/archive/` 为归档目录，回归扫描只扫 `audit_logs` 顶层，归档文件不参与落盘明文检查。
- 归档文件保留历史原貌（含当时的原始哈希），**请勿修改**。
- 今后如需轮换 HMAC 密钥，应先归档当前链、再写 `chain_restart` 重开，**不要直接替换密钥文件**。

---

## 4. 运行回归检查

**一键命令：**

```bash
secure_env/Scripts/python.exe privacy_audit_test.py
```

脚本末尾打印**三项结论**并返回退出码：
- **泄露数**：当前基线 `4`（含 4 项已知取舍）
- **负样本误伤数**：应为 `0`
- **落盘明文发现**：应为 `无`

**退出码含义（第 5 步新增）：**

| 退出码 | 含义 |
| --- | --- |
| `0` | 隐私回归检查通过（泄露≤4、误伤=0、落盘干净） |
| `1` | 任一失败：泄露数超阈 / 负样本误伤 / 落盘发现敏感明文 |

---

## 5. 五步修复脉络

面向维护者，说明为什么系统是现在这个样子：

1. **第 1 步 P0 热修** — 修复 `agent_worker.py` 四处"条件短路"：提问无敏感词时工具读到敏感内容不再绕过脱敏；强制工具调用走审批；审计不再落盘还原后的明文。
2. **第 2 步 统一脱敏通道** — 封装 `_anonymize_tool_output()`，把工具结果脱敏产生的映射并入跨轮映射表，消除占位符副作用；修复审计统计对含 nonce 占位符的识别。
3. **第 3 步 落盘面治理** — 附件/消息加密落盘并迁移存量明文；审计日志全字段掩码；会话恢复正确还原占位符；工作流报告落盘前脱敏。
4. **第 4 步 规则集扩充** — 真机审计泄露 22→4；新增 10 类实体规则与上下文精验；建立负样本回归（19 条零误伤），保证扩展不误伤正常业务。
5. **第 5 步 收尾加固** — MONEY 规则恢复裸"元"能力并保持零误伤；密钥移出项目目录 + 权限收紧 + DPAPI 包裹；存量明文报告重写、回环 IP 白名单；回归脚本退出码语义固化；本文档。

**核心原则**：脱敏只替换"发给模型"的中间文本，用户看到的始终是还原后的明文；严格精验控制误报；密钥与敏感明文不落到项目目录，且修复相互不破坏既有闭环。