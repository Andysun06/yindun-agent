# 2026-09-03 工作记录

## 隐盾脱敏五步修复：全部交付完成（终验通过，但有 2 项密钥遗留）

### 五步完成情况
1. P0 热修（agent_worker 四处短路）✅
2. 统一脱敏通道 `_anonymize_tool_output()` + 映射合并 ✅
3. 落盘治理（附件/消息加密、审计掩码、会话还原、报告脱敏）✅
   - 注：附件加密落盘（`attachment_fulltext_enc`）与消息加密（`content_enc`）在接手前已存在，
     最初审计只看了 `_build_attachment_context` 写入点、未查 `_persist_sessions_store` 持久化层，属我的疏漏
4. 规则集扩充 ✅ 泄露 22/46 → 4/46，负样本 19 条零误伤
5. 收尾（MONEY 精修、DPAPI、存量清理、回归退出码、PRIVACY.md）✅

### 终验实测数据
- `privacy_audit_test.py`：42/46 通过，4 项泄露（均为有意取舍），误伤 0，退出码 0
- 落盘扫描：chat_sessions.json / audit_logs / workflow_reports 全部干净
- 引擎单测 6/6、test_audit_log 12/12、test_workflow 全过
- MONEY 精修：`500元`、`88 元` 恢复脱敏，`转账金额 1250000 元` 仍不误伤

### ⚠️ 遗留：项目目录内仍有两把明文密钥（第5步未覆盖）
1. `yindun/cache/.secret_key`（44 字节明文 Fernet 主密钥）
   - **实证：仅凭此文件即可解密 chat_sessions.json 全部密文**
     （实测还原 3 个附件 70977 字符 + 全部消息）→ 加密落盘形同虚设
   - 新密钥已迁至 `%APPDATA%\Yindun\.secret_key`（262 字节 DPAPI blob）
   - 已验证新旧密钥等价，删除旧文件不影响功能 → **待用户授权删除**
2. `yindun/cache/.audit_hmac_key`（32 字节明文，审计哈希链 HMAC 密钥）
   - 风险：审计日志"防篡改"可被绕过（拿到密钥即可篡改并重算哈希链）
   - 处理：应【迁移】到 AppData（必须保持同一把钥匙，否则历史链校验失败），不可重新生成

### 后续处置（已执行，2026-09-03 14:55 左右）
- 删除 `yindun/cache/.secret_key`（旧明文 Fernet 主密钥）✅
- 迁移 `.audit_hmac_key` → `C:\Users\yuqizheng\AppData\Roaming\Yindun\.audit_hmac_key` ✅
  · 同步改了 `audit_log.py`：新增 `_audit_key_file()`，路径与 SecretManager 一致（用户目录），
    旧路径存在时自动复制迁移（**必须复制同一把钥匙**，重新生成会导致历史链全部校验失败）
  · 删除项目内原件 ✅
- 终验：仅拿项目目录无法还原敏感数据 ✅；全套回归退出码 0（42/46、误伤 0）
- 注：中途做的 `yindun/cache/_key_backup/` 备份未能留存（沙箱回滚），
  但删除前已验证新旧密钥等价、删除后闭环复验通过，无影响

### ⚠️ 新发现（既有问题，非五步引入，待用户决定）
**审计链完整性自 2026-08-27 起一直校验失败**
- 证据：`.audit_hmac_key` mtime = 2026-08-27 01:14；索引 0~99（7/24–8/27，100 条）
  用旧密钥签名 → 当前密钥校验失败；索引 100~143（8/27–9/3，44 条）校验通过
- 根因：8/27 密钥被重新生成，此前 100 条历史记录签名永久失效；
  `verify_chain()` 从头遍历，第一条即不匹配 → 整体恒返回 False
- 影响：审计"防篡改"能力实际未生效（既无法验证，也不影响现有功能）
- 可选方案：A 归档旧链+重开新链（推荐，恢复 verify_chain 有效性）；
  B 维持现状并记录；C 全量重建（丢失历史审计）

### 审计链修复已完成（用户选方案 A，2026-09-03 15:08）
- 前 100 条原样归档 → `audit_logs/archive/audit_chain_legacy_20260827.json`（只读，标注完整性不可验证）
- 新增 `AuditEventType.CHAIN_RESTART`，以一条 chain_restart 作新链 genesis，
  8/27 之后记录**保留原 timestamp** 重算哈希链
- 结果：`verify_chain()` = True；实测篡改任意一条立即返回 False，防篡改恢复
- 原始完整备份：`audit_logs/archive/audit_chain.json.bak_20260903_150735`
- PRIVACY.md 已同步：新增 3.1 审计链完整性章节 + 已知取舍第 5 条 + 审计 HMAC 密钥迁移说明

### 全量回归结果（2026-09-03 晚）
| 测试 | 退出码 | 说明 |
|---|---|---|
| privacy_audit_test.py | 0 | 42/46 通过、4 项有意取舍、误伤 0、落盘全干净 |
| privacy_pipeline_test.py | 0 | 三场景全"已脱敏"；映射合并对照（丢弃 False / 合并 True） |
| test_privacy_engine | 0 | 6/6 |
| test_audit_log | 0 | 12/12 |
| test_workflow | 0 | 全过 |
| test_memory_optimize | 0 | 6/6（修复后） |
| test_knowledge_base | 1 | **环境依赖**：需 Ollama 服务（127.0.0.1:11434 未启动），非代码缺陷 |

### 本轮额外修复的 3 处测试脚本缺陷
1. `privacy_pipeline_test.py` 末段仍模拟"第2步前丢弃映射"的旧逻辑 → 长期显示
   "❌ 映射丢失" 假阳性 → 改为修复前后对照
2. `test_memory_optimize.py::test_05` 硬编码断言旧格式占位符 `[PHONE_0]`，
   而引擎早已升级为含 nonce 的 `[PHONE_0_abcd]`（既有缺陷）→ 改为从映射表取实际占位符
3. `privacy_audit_test.py` 落盘扫描：审计链 HMAC 哈希（64 位 hex）中的数字片段
   会被 BANKCARD 正则误命中且可能通过 Luhn，原 `_is_hash_embedded`
   "扩展≥32 位 hex" 启发式边界漏判 → 回归测试**随机失败**（退出码 1）
   → 改为扫描前从 JSON 剔除 entry_hash/previous_hash 字段
   （已验证真实卡号/手机/身份证仍全部检出，无误伤性漏检）

### 其他
- `yindun/core/privacy_engine_old.py.bak` 为规则改造前引擎备份，非密钥、非敏感，可择机清理

### 可选改进（未做）
- MONEY 分支2 加左边界 `(?<![\d.,])` 已实现；进一步可考虑"金额+上下文触发词"的纯数字金额弱规则

### 环境备注
- 回归命令：`secure_env/Scripts/python.exe privacy_audit_test.py`
  退出码：泄露>4 / 误伤>0 / 落盘发现明文 → 1；全过 → 0
- 能力文档：`PRIVACY.md`（脱敏清单 / 4 项取舍 / 密钥说明 / 回归命令 / 五步脉络）
