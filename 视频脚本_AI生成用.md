# 隐盾安全智能体 · 演示视频脚本（AI 视频生成用）

> 用途：交给 AI 视频生成工具，按下面分镜逐镜产出并拼接成片。每镜给了【画面提示词】【旁白 VO】【屏幕字幕】【时长】【素材】。旁白文案可直接喂 TTS。

---

## 0. 全局规格
- **画幅**：16:9，1920×1080，30fps，成片时长 **约 4 分 30 秒**（≤5 分钟）。
- **风格**：科技·安全·克制。主色 盾牌绿 `#0E7A4F`，强调色 警示橙 `#E8590C`，背景 深墨绿 `#0B3D2E`（片头/片尾）与浅灰白 `#F4F7F5`（内容）。字体 微软雅黑。
- **配音**：中文男声 `zh-CN-YunxiNeural`（沉稳），语速 1.0；每段旁白见下。
- **字幕**：底部居中，白字+半透明深底条，逐句随旁白；关键数字用橙色高亮。
- **BGM**：低音量电子/科技感纯音乐（用户自备），旁白处压至 -18dB。
- **转场**：镜间统一 0.5s 交叉溶解；章节切换用 0.8s。
- **动效**：静态截图统一加缓慢 Ken Burns（3–5s 内缩放 100%→108% + 轻微平移），使截图看起来像录屏。

---

## 1. 分镜表

### 镜01 · 片头（0:00–0:14）
- 画面提示词：深墨绿背景，中央一枚发光的盾牌图标缓缓浮现，标题文字淡入；背景有极缓慢的数据流粒子。
- 屏幕文字：`隐盾安全智能体` ／ 副标题 `面向涉密办公场景的本地化隐私防护桌面智能体`
- 旁白：「隐盾安全智能体——让大模型在看不见敏感数据的前提下，安全地处理涉密办公任务。」

### 镜02 · 背景痛点（0:14–0:52）
- 画面提示词：深色科技底，三条"泄露面"以图标+文字依次滑入（云上传箭头 / 数据库磁盘 / 日志文件）。
- 屏幕文字：`出网 · 落盘 · 留痕 —— 三大泄露面`
- 旁白：「大模型进办公，但合同、薪酬、病历这类数据高度敏感。麻烦在于三条泄露面：明文随请求发到云端、会话和向量库把隐私写进磁盘、连审计日志本身都可能反成泄露源。我们的目标，是把隐私保护从靠人自觉，变成系统机制。」
- 素材：可纯动效图文，或叠 `01_主界面_会话列表.png`。

### 镜03 · 系统架构（0:52–1:20）
- 画面提示词：一张分层架构图，自下而上高亮"安全内核"四个模块。
- 屏幕文字：`三层架构：界面 · 推理引擎 · 安全内核`
- 旁白：「隐盾是常驻桌面的智能体，三层架构：界面、ReAct 推理引擎、以及安全内核。安全内核由隐私引擎、策略引擎、密钥管理、审计日志四块组成，覆盖输入到落盘到审计的全链路。」
- 素材：`架构图`（docs/screenshots/00_架构图.png）。

### 镜04 · 隐私网关原理（1:20–2:10）
- 画面提示词：左侧真实文本，右侧逐字变成彩色占位符 `[PHONE_0]` 等，中间一个"加密锁"图标；底部浮出公式 `A(x) → (x̂, M)`。
- 屏幕文字：`20+ 类实体 · 占位符化 · Fernet+DPAPI 加密 · nonce 防劫持`
- 旁白：「核心是这个内存级脱敏网关。它能识别二十多类敏感实体——手机号、身份证、银行卡、密钥、病历、金额、人名等。数据进模型前，先在内存里替换成占位符，真实值以 Fernet 加密、密钥再经系统级 DPAPI 保护；模型全程只看到占位符，回答返回时再自动还原。占位符还带随机 nonce，防止被植入伪造。」
- 素材：`02_隐私对话_回复.png`（对话里含脱敏与还原）。

### 镜05 · 纵深防御（2:10–2:50）
- 画面提示词：四道同心防线依次点亮：沙箱 → 权限 → 白名单 → 审批；随后切到红色审批弹窗特写。
- 屏幕文字：`路径沙箱 · 权限分级 · 命令白名单 · 熔断审批`
- 旁白：「不止隐私。所有写盘、删除、执行命令都被关进沙箱：越界路径直接拒绝，命令只放行白名单，高危操作会熔断挂起、弹窗等人工审批，超时自动驳回。每一步都进哈希链审计，篡改立刻能查出来，而审计自己也只存脱敏预览。」
- 素材：`03_审批弹窗.png` + `04_审计面板_概览.png`。

### 镜06 · 现场演示（2:50–3:52）★重点
- 画面提示词：真实操作录屏——输入含手机号/身份证的指令→发送→弹审批→授权→文件创建→回复还原明文。LLM 等待处 4× 快进。
- 屏幕文字：`输入含敏感信息 → 模型只见占位符 → 审批放行 → 落盘 → 还原`
- 旁白：「来看一条真实指令。我发送一段含姓名、手机号、身份证、邮箱的话，要求存成本地文件。模型收到的是占位符；它自主发起建文件工具，系统弹出审批，我授权；文件在沙箱里真实创建，回复里的数据自动还原。整个过程，云端模型没接触过任何真实敏感值。」
- 素材：**建议用 `Win+G` 现场录这段**（最有说服力）；退而用 `02_隐私对话_回复.png` + `03_审批弹窗.png` 串成伪录屏。

### 镜07 · 知识库 RAG（3:52–4:12）
- 画面提示词：设置页知识库列表（3 篇已入库）→ 聊天里提问→检索回答。
- 屏幕文字：`入库即脱敏 · 向量库零明文 · 语义检索`
- 旁白：「机密文档可以建本地知识库。文档在向量化前逐块脱敏，向量库里只存占位符，真实隐私永不落盘；检索命中后，答案里的占位符再还原给你。」
- 素材：`05_设置面板_算力与隐私.png` + 你补的 `08_知识库检索_*.png`。

### 镜08 · 攻击实验数据（4:12–4:32）
- 画面提示词：深色数据卡，四组关键数字翻牌式出现（100% / 0 / 0/500 / 0）。
- 屏幕文字：`黑盒召回 100% · 明文泄露 0 · 还原劫持 0/500 · 白盒持久化命中 0`
- 旁白：「隐私到底有没有用，我们做了可复现的攻防评测。在合成语料上：出网脱敏召回率 100%、明文泄露 0、还原劫持 500 轮成功 0、白盒拿走磁盘文件无密钥也恢复不出明文。」
- 素材：纯数据动效（数字来自 privacy_eval_results.json）。

### 镜09 · 展望 + 片尾（4:32–4:50）
- 画面提示词：深墨绿背景，三条展望逐行浮现，末尾回到盾牌 logo + slogan。
- 屏幕文字：`NER 增强 · 审计链外部锚定 · 工具调用结果二次核验` ／ 结尾 `让本地 AI 更安全、更智能、更可控`
- 旁白：「下一步，引入 NER 提升开放域召回、审计链外部锚定、以及工具调用的结果二次核验。隐盾——让本地 AI 更安全、更智能、更可控。」

---

## 2. 素材清单（对应 `素材/`）
| 文件 | 用在镜 |
|---|---|
| 01_主界面_会话列表.png | 02 |
| 01b_新建会话弹窗.png | （备用） |
| 02_隐私对话_回复.png | 04, 06 |
| 03_审批弹窗.png | 05, 06 |
| 04_审计面板_概览.png | 05 |
| 05_设置面板_算力与隐私.png | 07 |
| 06_工作流编排.png | （备用/可插入镜05后） |
| 08_知识库检索_*.png（待补） | 07 |
| docs/screenshots/00_架构图.png | 03 |

## 3. 给 AI 视频工具的备注
- 静态 PNG 一律加 Ken Burns（缩放 100→108% + 缓移），避免"幻灯片感"。
- 旁白与字幕以本文件【旁白】列为准，逐镜对齐时长。
- 若某镜有真实录屏（镜06/07），优先用录屏，其余用截图+动效。
- 数字/术语（100%、0/500、Fernet、DPAPI、nonce）保持原样，别改写。
